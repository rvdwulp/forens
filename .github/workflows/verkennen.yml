name: Verken databronnen

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verkennen:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node scripts/verkennen.js
